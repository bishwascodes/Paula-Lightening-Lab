name: Trigger auto deployment for resumeapp-web-api

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/resumeapp-web-api-AutoDeployTrigger-fb3e8f85-dd96-4000-9172-33d334cc736f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.RESUMEAPPWEBAPI_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.RESUMEAPPWEBAPI_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.RESUMEAPPWEBAPI_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: docker.io
          registryUsername: ${{ secrets.RESUMEAPPWEBAPI_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.RESUMEAPPWEBAPI_REGISTRY_PASSWORD }}
          containerAppName: resumeapp-web-api
          resourceGroup: ResumeApp
          imageToBuild: paulawenighofer/resumeapp-web-api:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


